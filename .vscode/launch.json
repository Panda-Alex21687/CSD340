{
  "version": "0.2.0",
  "configurations": [
    {
      "type": "chrome",
      "request": "launch",
      "name": "Debug Current HTML File",
      "file": "${file}",
      "webRoot": "${workspaceFolder}"
    }
  ]
}